import React from "react";

const MapFunction = (props) => {
    return (
        <div>
            <h4>Hello this is {props.name}</h4>
            <p>My contact {props.number}</p>
        </div>
    )
}

export default MapFunction;