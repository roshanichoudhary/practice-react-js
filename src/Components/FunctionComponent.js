import React from "react";

const FunctionComponent = (props) => {
  return (
    <h2>
      I am {props.modal} modal. My color is {props.color} color.
    </h2>
  );
};

export default FunctionComponent;
